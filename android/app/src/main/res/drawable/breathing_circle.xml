<?xml version="1.0" encoding="utf-8"?>
<!-- Circulo usado como guia visual de respiracion en PrayerGateActivity.
     Se anima (escala) programaticamente desde Kotlin. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="#33C9873A" />
    <stroke
        android:width="2dp"
        android:color="@color/gate_accent" />
    <size
        android:width="140dp"
        android:height="140dp" />
</shape>
